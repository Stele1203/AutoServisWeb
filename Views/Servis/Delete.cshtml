@model AutoServisWeb.Models.Servisi
@{ ViewBag.Title = "Obrisi servis"; }

<h2>Potvrda brisanja</h2>
<hr />
<p class="text-danger">Da li ste sigurni da zelite obrisati ovaj servis?</p>

<dl class="row">
    <dt class="col-sm-3">Vozilo</dt>
    <dd class="col-sm-9">@(Model.Vozila != null ? Model.Vozila.RegistarskaOznaka : "-")</dd>
    <dt class="col-sm-3">Datum</dt>
    <dd class="col-sm-9">@Model.DatumServisa.ToString("dd.MM.yyyy")</dd>
    <dt class="col-sm-3">Opis</dt>
    <dd class="col-sm-9">@Model.OpisRadova</dd>
    <dt class="col-sm-3">Cijena</dt>
    <dd class="col-sm-9">@Model.UkupnaCena.ToString("N2") RSD</dd>
</dl>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-danger">Obrisi</button>
    @Html.ActionLink("Odustani", "Index", null, new { @class = "btn btn-secondary ms-2" })
}